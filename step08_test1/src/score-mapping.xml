<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mybatis.scoreMapper">
	<insert id="insert_score" parameterType="test.bean.ScoreVO">
		<!-- 데이터 삽입 자리 : #{변수명} -->
		insert into score values (#{studNo}, #{name}, #{kor}, #{eng}, #{mat}, #{tot}, #{avg}, sysdate)
	</insert>
	
	<update id="update_score" parameterType="test.bean.ScoreVO">
		update score set name=#{name}, kor=#{kor}, eng=#{eng}, mat=#{mat}, tot=#{tot}, avg=#{avg} where studNo=#{studNo}
	</update>
	
	<delete id="delete_score" parameterType="String">
		delete score where studNo=#{studNo}
	</delete>
	
	<!-- resultType : 리턴값의 자료형을 나타냄 -->
	<select id="get_score" parameterType="String" resultType="test.bean.ScoreVO">
		select * from score where studNo=#{studNo}
	</select>
	
	<!-- resultType : 리스트에 저장될 자료형을 나타냄 -->
	<select id="get_scorelist" resultType="test.bean.ScoreVO">
		select * from score order by studNo asc
	</select>
</mapper>

	